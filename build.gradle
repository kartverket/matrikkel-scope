group = 'org.scopemvc'
version = projectVersion

apply plugin: 'java'
apply plugin: 'maven-publish'

sourceCompatibility = '1.7'

sourceSets {
  main {
    java.srcDirs = ['src']
    resources.srcDirs = [] //ingen ekstra resources på classpath
  }

  test {
    java.srcDirs = ['src.test']
    resources.srcDirs = [] //ingen ekstra resources på classpath
  }
}

dependencies {
  compile 'commons-logging:commons-logging:1.0.4'
  compile 'javax.servlet:servlet-api:2.5'
  compile 'javax.servlet.jsp:jsp-api:2.1'
  compile 'oro:oro:2.0.8'

  testCompile 'junit:junit:3.8.1'
  testCompile files('lib/jfcunit.jar')
}

repositories {
  maven {
    name = 'public'
    url project.ext.properties.get('MAVEN_REPO', 'http://nexus.statkart.no:8090/nexus/content/groups/public')
  }
}

publishing {
  publications {
    mavenJava(MavenPublication) {
      from components.java
    }
  }
  repositories {
    maven {
      url NEXUS_BASE_URL + "/${project.version.endsWith('-SNAPSHOT') ? 'snapshots' : 'releases'}/"
      credentials {
        username NEXUS_USERNAME
        password NEXUS_PASSWORD
      }
    }
  }
}
