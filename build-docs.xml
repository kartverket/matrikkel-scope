<project name="XDoclet Documentation" default="all-docs" basedir=".">

    <property name="scope.root.dir" value="${basedir}"/>
    <property file="${scope.root.dir}/build.properties"/>

    <!-- =================================================================== -->
    <!-- Generate all the docs                                               -->
    <!-- =================================================================== -->
    <target name="all-docs" depends="site,api"/>

    <!-- =================================================================== -->
    <!-- Define classpath                                                    -->
    <!-- =================================================================== -->
    <path id="doc.class.path">
        <fileset dir="${lib.dir}">
            <include name="**/*.jar" />
        </fileset>
        <fileset dir="${dist.lib.dir}">
            <include name="**/*.jar" />
        </fileset>
    </path>

    <target name="sflogo-online" if="scope.sflogo.online">
        <property name="docs.sflogo.src" value="${docs.sflogo.src.online}"/>
    </target>
    <target name="sflogo-offline" unless="scope.sflogo.online">
        <property name="docs.sflogo.src" value="${docs.sflogo.src.offline}"/>
    </target>

    <!-- =================================================================== -->
    <!-- Creates the Web Site documentation                                  -->
    <!-- =================================================================== -->
    <target name="site" depends="sflogo-online,sflogo-offline">
        <taskdef 
            name="anakia" 
            classname="org.apache.velocity.anakia.DynamicAnakiaTask" 
            classpathref="doc.class.path"
         />
        <!-- Remove characters than may break xml-->
        <copy file="history.txt" tofile="${build.dir}/history.txt" overwrite="true" />
        <replace dir="${build.dir}" includes="history.txt">
          <replacefilter token="&amp;" value="&amp;amp;"/>
          <replacetoken><![CDATA[<]]></replacetoken><replacevalue><![CDATA[&lt;]]></replacevalue>
          <replacetoken><![CDATA[>]]></replacetoken><replacevalue><![CDATA[&gt;]]></replacevalue>
        </replace>
        <loadfile property="CHANGES" srcFile="${build.dir}/history.txt" />
        <copy file="xdocs/changes.tmpl" tofile="xdocs/changes.xml" overwrite="true">
          <filterset>
            <filter token="CHANGES" value="${CHANGES}" />
          </filterset>
        </copy>

        <anakia 
            basedir="${docs.src.dir}" 
            destdir="${docs.build.dir}/"
            extension=".html" 
            style="./site.vsl"
            projectFile="resources/project.xml"
            excludes="**/resources/** empty.xml"
            includes="**/*.xml"
            lastModifiedCheck="true"
            templatePath="${docs.src.dir}/stylesheets"
            encoding="utf-8"
            
            sflogosrc="${docs.sflogo.src}"
        >
        </anakia>

        <copy todir="${docs.build.dir}" filtering="no">
            <fileset dir="${docs.src.dir}">
                <include name="**/*.gif"/>
                <include name="**/*.jpeg"/>
                <include name="**/*.jpg"/>
                <include name="**/*.png"/>
                <include name="**/*.js"/>
                <include name="**/*.css"/>
                <include name="**/*.txt"/>
                <include name="**/*.html"/>
            </fileset>
        </copy>
    </target>

    <!-- =================================================================== -->
    <!-- Creates the API documentation                                       -->
    <!-- =================================================================== -->
    <target name="api">
        <mkdir dir="${docs.api.dir}"/>
        <javadoc packagenames="${javadoc.packages}"
                 sourcepath="${src.dir}"
                 destdir="${docs.api.dir}"
                 author="yes"
                 version="yes"
                 use="yes"
                 source="1.8"
                 private="false"
                 windowtitle="${Name} API"
                 doctitle="${Name}"
                 bottom="${javadoc.copyright}"
                 overview="${src.dir}/overview.html">
            <classpath refid="doc.class.path"/>
            <link href="https://docs.oracle.com/javase/8/docs/api/"/>
            <link href="http://java.sun.com/j2ee/j2sdkee/techdocs/api/"/>
        </javadoc>
    </target>

</project>
